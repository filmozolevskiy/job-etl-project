feat: Add React frontend with production Docker setup and fix test infrastructure

## Frontend (React UI)
- Add complete React frontend application with TypeScript
- Configure Docker production build using Nginx
- Add frontend service to docker-compose with port mapping
- Configure Vite proxy for API calls to campaign-ui backend
- Remove unnecessary React imports (JSX transform)
- Fix TypeScript type issues in components and tests
- Add nginx configuration for SPA routing

## Testing Infrastructure Fixes
- Fix database cleanup in integration tests (terminate connections before cleanup)
- Fix dbt environment variable inheritance in test_helpers.py
- Fix Flask dependencies in requirements.txt (Flask, flask-cors, flask-jwt-extended)
- Fix JWT authentication in cover letter generation tests
- Fix migration script conditional comments (99_fix_campaign_id_uniqueness.sql)

## dbt Model Improvements
- Add field normalization to staging.jsearch_job_postings model
- Normalize job_employment_type and employment_types to UPPERCASE
- Normalize job_salary_period to lowercase
- Ensure consistent field casing per Bug #4 standards

## Test Results
- Frontend tests: 12/12 passed
- Backend tests: 450/452 passed (2 dbt incremental logic tests require refactoring)
- Ruff linting: All checks passed
